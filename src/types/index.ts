export interface UseCase {
  domain: string;
  section: string;
  skill_level: string;
  skill_emoji: string;
  title: string;
  prompt: string;
  tags: string[];
  best_for: string;
  source: string;
}
